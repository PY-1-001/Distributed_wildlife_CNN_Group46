# Distributed_wildlife_CNN_Group46
This project implements a Convolutional Neural Network (CNN) using PyTorch to classify images from the Animals10 dataset. The pipeline includes dataset preprocessing, train-validation split, GPU-accelerated training, and performance evaluation. The model is trained using Adam optimizer and CrossEntropy loss &amp;supports CUDA for efficient computation.
